spring:
  datasource:
    url: jdbc:postgresql://viaduct.proxy.rlwy.net:11073/railway
    username: ${PGUSER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    show-sql: false
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

